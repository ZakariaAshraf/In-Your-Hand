/// Central place for app display name and version.
/// Update these when rebranding or for deployment; keep in sync with pubspec.yaml version.
class AppInfo {
  AppInfo._();

  /// Application display name (e.g. for About, splash, store listing).
  static const String appName = 'In Your Hand';

  /// Version string shown in UI (e.g. "1.0.0"). Update with pubspec.yaml version.
  static const String version = '1.0.0';
}
